pip install "numpy<2" onnxruntime==1.12.0 "opencv-contrib-python>=4.6.0.66" tensorflow==2.10.0 torch==1.12.0 "tf2onnx>=1.12.0" "simple-onnx-processing-tools>=1.0.54" "scikit-learn>=1.1.0" "matplotlib>=3.5.0"

protobuff==3.20.3 shows dependencies error but the code runs


1. install edge tpu runtime
# Add Coral repository
echo "deb https://packages.cloud.google.com/apt coral-edgetpu-stable main" | sudo tee /etc/apt/sources.list.d/coral-edgetpu.list

# Add Google's package signing key
curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -

# Update package list
sudo apt-get update

# Install Edge TPU runtime (standard version - safer for Pi)
sudo apt-get install libedgetpu1-std

# Install Python library for Edge TPU
pip3 install pycoral

2. check if coral tpu is detected

lsusb | grep "Global Unichip"

3. run code